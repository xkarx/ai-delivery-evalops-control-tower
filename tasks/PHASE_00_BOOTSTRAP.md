# Phase 00: Bootstrap

Deliver monorepo structure, shared schemas, adapter interfaces and mocks, database migrations, workflow state, CI baseline, design system, `.env.example`, setup scripts, health checks, architecture decisions, and implementation status.

Acceptance: fresh clone installs; lint, type checks, and tests pass; control tower shell and mock integration health page render.
