# Phase 02: Company Data

Deliver deterministic generator, company folders, schemas, validators, seed scripts, scenario manifest, data dictionary, data browser, and shared IDs.

Acceptance: minimum dataset exists; same seed reproduces it; invalid references fail; evidence contains noise and conflicts.
