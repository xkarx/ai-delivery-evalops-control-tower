Implement the selected phase under `tasks/`.

Before coding, review authoritative context, dependencies, status, requirement IDs, and tests.

During work, use adapters, preserve demo mode, add visual states and telemetry, document behavior, and never commit credentials.

Before completion, run checks, update status and traceability, and summarize changed files, test evidence, risks, assumptions, and manual connection steps.
